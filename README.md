## How to Run (Local)

### Prereqs
- Python venv: `.venv` (already set up)
- Ollama running locally
- Postgres running locally + pgvector enabled
- API key set in `.env`

### 1) Start Ollama
In one terminal tab:
```bash
ollama serve